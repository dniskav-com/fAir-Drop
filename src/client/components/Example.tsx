import React from 'react'

export default function Example(): JSX.Element {
  return (
    <div className="react-example">
      <strong>Componente React activo</strong>
      <p>Esto confirma que .tsx está compilando y bundling con esbuild.</p>
    </div>
  )
}
